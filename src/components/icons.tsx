import { Loader2, AlertCircle, Chrome, Eye, EyeOff } from "lucide-react"

export const Icons = {
  spinner: Loader2,
  alertCircle: AlertCircle,
  google: Chrome, // Using Chrome icon as a fallback for Google
  eye: Eye,
  eyeOff: EyeOff,
}
